<div class="container" style="margin-top:30px">
	<div class="row">
    <div class="col s12">
      <div class="card">
        <div class="card-content">
					<%= form_with url: fishtagram_comment_path, method: "patch",scope: :comment, local: true do |form| %>
					  <p>
					  	Comment:<br>
					    <%= form.text_area :body %>
					  </p>

					  <p>
					    <%= form.submit %>
					  </p>
					<% end %>
					<%= link_to 'Home page', root_path %>
				</div>
			</div>
		</div>
	</div>
</div>